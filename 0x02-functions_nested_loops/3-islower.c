#include "main.h"
/**
 * _islower - checks for lowercase character
 *
 * Return: 1 if c is lowercase
 * 0 otherwise
 */
int _islower(int c)
{
if (c <= 'c')
{
return (1);
}
else if (c <= 'o')
{
return (1);
}
else if (c <= 108)
{
return (0);
else if (c <= 'I')
{
return (1);
