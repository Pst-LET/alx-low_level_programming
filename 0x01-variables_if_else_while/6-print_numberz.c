#include <stdio.h>
/**
 * main - prints numbers in base 10
 *
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 48; num <= 57; ++num)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
